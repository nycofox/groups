<nav class="bg-amber-200 p-6 shadow-lg w-full fixed">
    <div class="container mx-auto">
        <a href="{{ route('home') }}" class="text-lg font-bold">
            {{ config('app.name') }}
        </a>
    </div>
</nav>
